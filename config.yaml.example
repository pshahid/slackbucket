cs-cofc:
    channels:
      - cscofc
    plugins:
      - factoids.plugin.Factoids
    database:
      name: bucket
      port: 5432
      host: bucket-postgresql.bucket.svc.cluster.local
      vendor: postgresql
